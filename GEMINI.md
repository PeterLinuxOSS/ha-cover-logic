# GEMINI.md

Read **[`MODELS.md`](MODELS.md)** first. It is the single source of truth
for this project: what it is, its pure-core/Home-Assistant-layer split,
the decision model, the configuration format, the migration gate, the
deliberate decisions that look like bugs, current phase status, how to run
the tests (two interpreters — see §8), and the rules for changing this
code (§9, most importantly "parity first, improvements later").

Nothing here duplicates that content, so there is only one place for it to
go stale.

Reminder specific to this rule set: never add a `claude.ai` session link to
a commit message in this repository.
